import { renderTemplate } from './template';
import {
    isObject,
    isArray,
    isString,
    isNumber,
    isBoolean,
    isFunction
} from './typecheck';

const utilities = {
    renderTemplate,
    
    isObject,
    isArray,
    isString,
    isNumber,
    isBoolean,
    isFunction,
};

export default utilities;