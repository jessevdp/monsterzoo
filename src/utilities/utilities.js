import {
    isObject,
    isArray,
    isString,
    isNumber,
    isBoolean,
    isFunction
} from './typecheck';

import excludeProperties from './excludeProperties';

const utilities = {
    isObject,
    isArray,
    isString,
    isNumber,
    isBoolean,
    isFunction,

    excludeProperties,
};

export default utilities;